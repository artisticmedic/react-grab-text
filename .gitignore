node_modules/
# dist/ is committed on purpose: with no npm release, jsDelivr serves the
# script-tag build straight from a tag in this repo. Rebuild before tagging.
demo/dist/
test-results/
playwright-report/
.DS_Store
*.log
.chrome-profile/
.serve/
